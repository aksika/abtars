[
  {
    "pattern": "FTS index",
    "instruction": "Run: agentbridge-edit --rebuild-fts and report the result in one line.",
    "cooldownMin": 30,
    "enabled": true
  },
  {
    "pattern": "database disk image is malformed",
    "instruction": "Run: agentbridge-edit --rebuild-fts and report the result in one line.",
    "cooldownMin": 30,
    "enabled": true
  }
]
