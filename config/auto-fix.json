[
  {
    "pattern": "FTS index",
    "instruction": "Run: abtars-edit --rebuild-fts and report the result in one line.",
    "cooldownMin": 30,
    "enabled": true
  },
  {
    "pattern": "database disk image is malformed",
    "instruction": "Run: abtars-edit --rebuild-fts and report the result in one line.",
    "cooldownMin": 30,
    "enabled": true
  }
]
